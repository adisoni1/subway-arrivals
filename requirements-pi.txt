# Raspberry Pi runtime deps (data only). The LED driver itself —
# rpi-rgb-led-matrix — is built from source, not pip-installed; see the README.
# RGBMatrixEmulator is intentionally omitted (not needed on real hardware).
nyct-gtfs>=1.1.0
requests>=2.28.0
